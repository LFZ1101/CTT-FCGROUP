import {Body,Controller,Get,Post,UseGuards} from '@nestjs/common'; import {AuthGuard} from '../../common/guards/auth.guard'; import {CurrentUser,AuthUser} from '../../common/decorators/current-user.decorator'; import {SourcesService} from './sources.service'; import {CreateSourceDto} from './dto/source.dto'; @UseGuards(AuthGuard) @Controller('sources') export class SourcesController{constructor(private s:SourcesService){} @Get()list(@CurrentUser()u:AuthUser){return this.s.list(u.tenantId)} @Post()create(@CurrentUser()u:AuthUser,@Body()d:CreateSourceDto){return this.s.create(u.tenantId,d)}}
