import {Body,Controller,Get,Post,UseGuards} from '@nestjs/common'; import {AuthGuard} from '../../common/guards/auth.guard'; import {CurrentUser,AuthUser} from '../../common/decorators/current-user.decorator'; import {UnionsService} from './unions.service'; import {CreateUnionDto} from './dto/union.dto';
@UseGuards(AuthGuard) @Controller('unions') export class UnionsController{constructor(private s:UnionsService){} @Get() list(@CurrentUser()u:AuthUser){return this.s.list(u.tenantId)} @Post() create(@CurrentUser()u:AuthUser,@Body()d:CreateUnionDto){return this.s.create(u.tenantId,d)}}
