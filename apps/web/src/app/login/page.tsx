'use client'; import {FormEvent,useState} from 'react'; import {useRouter} from 'next/navigation'; import {api} from '../../lib/api';
export default function Login(){const r=useRouter();const [mode,setMode]=useState<'login'|'setup'>('login');const [error,setError]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError('');const f=new FormData(e.currentTarget);try{const data:any=await api(mode==='login'?'/auth/login':'/auth/bootstrap',{method:'POST',body:JSON.stringify(mode==='login'?{email:f.get('email'),password:f.get('password')}:{tenantName:f.get('tenantName'),name:f.get('name'),email:f.get('email'),password:f.get('password')})});localStorage.setItem('cct_token',data.accessToken);localStorage.setItem('cct_user',JSON.stringify(data.user));r.push('/')}catch(err:any){setError(err.message)}}return <div className="loginpage"><section className="loginvisual"><div className="brand" style={{padding:0}}><div className="brandmark">CI</div><span>CCT Intelligence</span></div><div><div className="eyebrow" style={{color:'#6f7c8d'}}>INTELIGÊNCIA TRABALHISTA</div><h1>Da publicação da CCT à ação do DP.</h1><p>Centralize sua carteira, monitore fontes, valide instrumentos e transforme mudanças coletivas em tarefas rastreáveis.</p></div><small style={{color:'#566271'}}>Plataforma oficial · ambiente seguro multi-tenant</small></section><section className="loginform"><div className="loginbox"><div className="eyebrow">Acesso seguro</div><h2>{mode==='login'?'Entre no workspace':'Criar primeiro workspace'}</h2><p>{mode==='login'?'Use seu e-mail corporativo para continuar.':'Configuração inicial do escritório e usuário proprietário.'}</p><form onSubmit={submit}>{mode==='setup'&&<><input name="tenantName" placeholder="Nome do escritório" required/><input name="name" placeholder="Seu nome" required/></>}<input name="email" type="email" placeholder="E-mail" required/><input name="password" type="password" placeholder="Senha · mínimo 8 caracteres" minLength={8} required/>{error&&<div style={{fontSize:11,color:'#b91c1c'}}>{error}</div>}<button className="primary">{mode==='login'?'Entrar':'Criar workspace'}</button></form><div className="hint">{mode==='login'?<>Primeiro acesso? <b onClick={()=>setMode('setup')} style={{cursor:'pointer',color:'#111827'}}>Criar workspace inicial</b></>:<>Já possui conta? <b onClick={()=>setMode('login')} style={{cursor:'pointer',color:'#111827'}}>Entrar</b></>}</div></div></section></div>}
