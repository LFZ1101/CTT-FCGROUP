'use client'; import {useEffect,useState} from 'react'; import Shell from '../components/Shell'; import PageHeader from '../components/PageHeader'; import {api} from '../lib/api';
const fallback={metrics:{companies:0,instruments:0,pendingValidations:0,unreadAlerts:0,openTasks:0},recent:[]};
export default function Home(){const [d,setD]=useState<any>(fallback);useEffect(()=>{api('/dashboard').then(setD).catch(()=>{})},[]);const m=d.metrics;return <Shell title="Visão geral"><div className="page"><PageHeader eyebrow="Central de comando" title="Inteligência trabalhista" description="Acompanhe o que mudou, o que exige validação e onde sua equipe precisa agir."/><section className="metrics">{[['Empresas monitoradas',m.companies,'carteira ativa'],['Instrumentos ativos',m.instruments,'CCT, ACT e aditivos'],['Aguardando validação',m.pendingValidations,'revisão humana'],['Alertas não lidos',m.unreadAlerts,'mudanças e divergências'],['Tarefas abertas',m.openTasks,'operação do DP']].map(([k,v,f])=><article className="metric" key={String(k)}><div className="k">{k}</div><div className="v">{v}</div><div className="f">{f}</div></article>)}</section><div className="grid2"><section className="panel"><div className="panelhead"><h2>Movimentações recentes</h2><span>fontes monitoradas</span></div><div className="feed">{d.recent?.length?d.recent.map((x:any)=><div className="feedrow" key={x.id}><span className={`sev ${x.severity==='CRITICAL'?'red':x.severity==='WARNING'?'amber':'blue'}`}/><div><div className="feedtitle">{x.title}</div><div className="feedmeta">{x.company?.tradeName||x.company?.legalName||x.instrument?.title||x.message}</div></div><div className="time">{new Date(x.createdAt).toLocaleDateString('pt-BR')}</div></div>):<div className="empty">A atividade aparecerá aqui conforme fontes, instrumentos e alertas forem cadastrados.</div>}</div></section><aside className="panel"><div className="panelhead"><h2>Fluxo operacional</h2><span>base oficial</span></div><div className="attention"><div className="attn"><strong>1. Cadastre a carteira</strong><p>Empresas, CNAEs, municípios e contexto operacional.</p></div><div className="attn"><strong>2. Vincule sindicatos e fontes</strong><p>Mediador, sindicato laboral, patronal e demais fontes oficiais.</p></div><div className="attn"><strong>3. Valide instrumentos</strong><p>A aplicação jurídica permanece rastreável e aprovada por humano.</p></div></div></aside></div></div></Shell>}
