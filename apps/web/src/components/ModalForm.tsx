'use client'; import {ReactNode} from 'react'; export default function ModalForm({open,title,children,onClose}:{open:boolean,title:string,children:ReactNode,onClose:()=>void}){if(!open)return null;return <div onMouseDown={onClose} style={{position:'fixed',inset:0,background:'rgba(8,12,18,.5)',zIndex:20,display:'grid',placeItems:'center',padding:20}}><div onMouseDown={e=>e.stopPropagation()} className="panel" style={{width:'min(720px,100%)',maxHeight:'90vh',overflow:'auto'}}><div className="panelhead"><h2>{title}</h2><button onClick={onClose} className="iconbtn">×</button></div><div style={{padding:18}}>{children}</div></div></div>}
