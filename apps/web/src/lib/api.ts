export const API_URL=process.env.NEXT_PUBLIC_API_URL||'http://localhost:4000/api/v1';
export async function api<T>(path:string,init:RequestInit={}){ const token=typeof window!=='undefined'?localStorage.getItem('cct_token'):null; const r=await fetch(`${API_URL}${path}`,{...init,headers:{'Content-Type':'application/json',...(token?{Authorization:`Bearer ${token}`}:{}) ,...(init.headers||{})}}); if(!r.ok){const e=await r.json().catch(()=>({message:'Erro de comunicação'})); throw new Error(Array.isArray(e.message)?e.message.join(', '):e.message||'Erro');} return r.json() as Promise<T>; }
